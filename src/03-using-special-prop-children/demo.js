import React from 'react'
import BoldText from './index.js'

export default (
  <BoldText>
    That's a text in bold passed as `children` prop to a BoldText component that's wrapping it with a strong tag.
  </BoldText>
)